'use strict';

// ===================== DATA =====================

const PALM_LINES = [
  { id:'life',     name:'生命線',  nameEn:'Life Line',       icon:'❤️',  path:[[32,22],[22,40],[18,58],[20,75],[24,85]] },
  { id:'head',     name:'知能線',  nameEn:'Head Line',       icon:'🧠',  path:[[30,36],[42,38],[55,40],[65,44],[72,48]], dash:[8,4] },
  { id:'heart',    name:'感情線',  nameEn:'Heart Line',      icon:'💜',  path:[[20,22],[35,18],[50,17],[65,18],[75,22]] },
  { id:'fate',     name:'運命線',  nameEn:'Fate Line',       icon:'⭐',  path:[[48,82],[48,65],[47,48],[46,32],[45,18]] },
  { id:'sun',      name:'太陽線',  nameEn:'Sun Line',        icon:'☀️',  path:[[65,55],[65,42],[64,30],[63,18]], dash:[6,4] },
  { id:'marriage', name:'結婚線',  nameEn:'Marriage Line',   icon:'💍',  path:[[78,20],[72,20],[68,20]], dash:[5,3] },
  { id:'money',    name:'金運線',  nameEn:'Money Line',      icon:'💰',  path:[[55,58],[58,48],[60,38],[62,28]], dash:[4,4] },
  { id:'popular',  name:'人気線',  nameEn:'Popularity Line', icon:'✨',  path:[[72,42],[70,35],[68,25]], dash:[3,5] },
  { id:'charm',    name:'モテ線',  nameEn:'Charm Line',      icon:'🌹',  path:[[38,44],[36,36],[35,26]], dash:[4,4] },
  { id:'devotion', name:'つくし線',nameEn:'Devotion Line',   icon:'🌿',  path:[[26,52],[30,46],[34,40]], dash:[5,5] },
];

const READINGS = {
  life: {
    scores:[72,85,91,68,78,82,88,75,65,93],
    texts:[
      '生命線が手首までしっかりと伸び、強靭な生命力を持っています。健康への意識が高く、困難な状況でも驚くほどの回復力を発揮するでしょう。線の弧が大きく、活発で行動的な人生を示しています。',
      '深く刻まれた生命線は、旺盛な活力と意志の強さの表れです。大きな病気にかかりにくい体質で、精神的にも肉体的にもタフ。人生の節目で力強く立ち上がれる底力があります。',
      '生命線は曲線が美しく、バイタリティが豊か。中年以降に人生の充実期が訪れることを示唆しています。',
    ],
    deep:{
      title:'生命線・詳細鑑定',
      content:[
        '生命線は単なる寿命の線ではなく、活力・生きるエネルギーの質を映す鏡です。あなたの生命線の深さは、日常のストレスに屈しない精神的な芯の強さと、高い免疫力を同時に示しています。',
        '線の途中に見られる小さな枝線は、人生の中で何度か大きな転機を迎えることを暗示しています。しかしその分岐点のたびに、より豊かな方向へ歩みを進める力が備わっています。',
        '親指の付け根（金星丘）への広がりが大きいほど、愛情の器が深く、パートナーへの献身度が高いことを意味します。愛する人のためなら底力が何倍にも引き出されるタイプです。',
      ],
      advice:['年1〜2回、自然の中でゆっくり過ごす時間が生命エネルギーをリチャージします','睡眠の質を高める工夫（就寝前のルーティン）が効果的です','好きな人・ものと過ごす時間があなたの源泉です','水分補給と適度な運動で健康運がさらにアップします'],
    }
  },
  head: {
    scores:[88,72,65,90,83,79,71,86,92,77],
    texts:[
      '知能線が長く横に伸び、論理的思考力と分析力に長けています。直感と理論のバランスが取れており、問題解決能力が高い。クリエイティブな仕事で才能を発揮できます。',
      '知能線がわずかに下向きで、豊かな想像力とロマンチックな一面を示しています。現実的な判断力と夢見る力を兼ね備えたバランス型の知性の持ち主。',
      '鮮明で深い知能線は集中力と記憶力の高さを示します。一度興味を持ったことは徹底的に掘り下げる探求心があります。',
    ],
    deep:{
      title:'知能線・詳細鑑定',
      content:[
        'あなたの知能線は、知的好奇心の旺盛さと多才な思考回路を示しています。線の長さは思考の射程距離——どれだけ広い視野で物事を捉えられるかを示し、平均より優れた洞察力を持つことを意味しています。',
        '知能線と感情線の間の「ます目」の広さが適度で、熱意を持ちつつ冷静に行動に移せるバランス感覚があります。交渉や調整の場面でとても有利に働きます。',
        '線の末端の形も重要です。先が二股に分かれている場合は異なる立場からの思考が得意で、これは交渉や調整の場面で有利に働きます。',
      ],
      advice:['毎日15分の読書やインプットの時間が知能線を輝かせます','新しい分野への挑戦が潜在的な才能を引き出します','アウトプット（書く・話す・教える）を意識すると思考が深まります','マインドマップや図解で思考を整理するとあなたの才能が最大化されます'],
    }
  },
  heart: {
    scores:[85,92,78,71,88,65,90,73,82,95],
    texts:[
      '感情線が鮮明で長く、豊かな感受性と深い愛情を持つ人です。感情表現が豊かで周囲の人を惹きつける魅力があります。恋愛においては一途で情熱的なタイプ。',
      '感情線が手のひら全体に広がるように伸びており、博愛精神と高い共感力を示しています。人の気持ちを察する能力が高く、自然とリーダー的な存在になっていきます。',
      '深い感情線は愛情の深さと情熱の強さを表しています。一度好きになると真剣に向き合う誠実なタイプ。その純粋さが相手の心を動かします。',
    ],
    deep:{
      title:'感情線・詳細鑑定',
      content:[
        '感情線はその人の愛の形と感情エネルギーの使い方を映し出します。あなたの感情線は豊かな感情の波と、それを相手に伝えようとする誠実さを同時に示しています。',
        '線の流れを詳しく見ると、中ほどで若干の揺らぎが見られます。これは感情的な試練を経験し、それによって感情の深みが増したことを示しています。その経験があなたをより繊細で思いやりのある人間に育てています。',
        '感情線が人差し指の付け根方向にしっかり向かっていることは、理想主義的な愛を追い求める傾向があることを意味します。妥協よりも本物の絆を求める姿勢は、現代では希少な価値ある資質です。',
      ],
      advice:['感情を言葉にして伝える練習がより深い絆を生み出します','自分の感情を日記に書くことで感情線のエネルギーが整います','相手の気持ちを聞く前に自分の気持ちを確認する習慣を','感情の波が激しい時期は創作や音楽などで昇華するのがおすすめです'],
    }
  },
  fate: {
    scores:[76,88,93,70,82,65,91,78,85,72],
    texts:[
      '運命線がはっきりと刻まれており、目標に向かって着実に進む力があります。30代以降に本来の力が開花し、人生の大きな転換点を迎えるでしょう。',
      '運命線が手首の近くから始まり、早くから自分の道を歩んできた自立した魂の持ち主です。困難な状況でも自力で道を切り拓く強さがあります。',
      '途中から現れる運命線は、大きな変化をきっかけに才能が開花するタイプ。遅咲きに見えても最終的に大きく花開く運命です。',
    ],
    deep:{
      title:'運命線・詳細鑑定',
      content:[
        '運命線は「自分らしい人生を生きているか」を映す指針です。あなたの運命線は、迷いながらも確かに前進してきた軌跡を刻んでいます。',
        '線の起点から、これまで多くの選択を自らの意志で行ってきたことが読み取れます。自力開拓型であり、環境に流されず自分の信念に従って歩んできた証拠です。',
        '線の途中に見られる枝は人生の岐路を示しています。複数の才能と選択肢があり、どの道を選んでも自分なりの成功を手にできる多才さがあります。',
      ],
      advice:['5年後の自分のビジョンを具体的に描くことが運命線を強化します','本業以外のスキルアップが運命の扉を予想外の方向から開けます','直感に従った行動があなたの運命線を輝かせる最大のカギです','信頼できるメンターを持つことで運命の流れが加速します'],
    }
  },
  sun: {
    scores:[80,93,75,88,70,92,67,85,91,78],
    texts:[
      '太陽線が美しく伸びており、才能と個性が開花して多くの人に認められる運を持っています。クリエイティブな分野での成功と名声が約束されています。',
      '太陽線の存在自体が「人に与える影響力」を示し、あなたには周囲を明るくしインスピレーションを与える特別な光があります。',
      '太陽線がくっきりと刻まれており、努力が正当に評価され名声と成功を手にする運命。40代以降に特に輝きが増すでしょう。',
    ],
    deep:{
      title:'太陽線・詳細鑑定',
      content:[
        '太陽線（アポロ線）は、その人が世の中にどれだけ輝けるかを示す線です。この線がある人は、社会での認知と成功の土台を持って生まれた幸運な存在です。',
        'あなたの太陽線は薬指の付け根に向かってしっかりと伸びており、芸術的・審美的なセンスと、それを通じた社会的評価を示しています。',
        '太陽線の輝きを最大化するのは「見せる勇気」です。SNS、創作活動、プレゼンテーションなど、自分を表現する機会を積極的に作ることで太陽線はさらに力強く刻まれていきます。',
      ],
      advice:['創作物を定期的に外部に発信することで才能が開花します','「人に見せる」ことへの恐れを手放すと成功への扉が開きます','美意識を磨く（美術館・音楽・デザインに触れる）ことが運を高めます','自分のブランディングを意識した活動が名声運を引き寄せます'],
    }
  },
  marriage: {
    scores:[83,70,92,77,88,65,80,95,72,85],
    texts:[
      '結婚線が明確に1本存在し、深く真剣な愛に恵まれる相を示しています。一生に一度の大きな愛と安定した結婚生活が待っています。',
      '結婚線が2本見られ、人生において2つの深い愛の縁があることを暗示。どちらも本物の愛であり、あなたの人生を豊かに彩るでしょう。',
      '結婚線が上向きにカーブしており、結婚後に運気が上昇する「格上げ婚」の相。パートナーとともに成長し互いを高め合える素晴らしい関係が待っています。',
    ],
    deep:{
      title:'結婚線・詳細鑑定',
      content:[
        '結婚線（小指の付け根付近の横線）は、深い愛の縁と生涯のパートナーとの絆を読む重要な線です。あなたの結婚線は形と位置の両方から豊かな愛情運を示しています。',
        '線の位置がやや高めなことから、焦らずじっくりと本当の相手を見つけるタイプです。条件よりも心のつながりを重視し、直感で「この人だ」と感じた相手と深い絆を築いていくでしょう。',
        '線の長さと深さから、パートナーシップにおける誠実さと粘り強さが読み取れます。関係を長く丁寧に育てる力があります。',
      ],
      advice:['「理想のパートナー像」を具体的に書き出すと縁が引き寄せられます','新しい出会いの場に月1回は足を運ぶ習慣を作りましょう','自分磨き（内面・外見両方）への投資が最高のパートナーを引き寄せます','感謝の気持ちをこまめに言葉で伝えましょう'],
    }
  },
  money: {
    scores:[78,85,90,72,65,93,80,88,75,82],
    texts:[
      '金運線が明確に存在し、財運に恵まれた相を示しています。本業以外にも収入を得るチャンスがあり、副業や投資での成功を示す吉相です。',
      '財運線が複数本見られ、多方面からの収入が見込める恵まれた手相です。特に40代以降に金運が大きく花開くことが期待されます。',
      '太く深い金運線は堅実な蓄財能力を示しています。努力した分だけ確実に報われる着実型の財運の持ち主です。',
    ],
    deep:{
      title:'金運線・詳細鑑定',
      content:[
        '金運線（財運線）は、お金をどのように引き寄せ、使い、増やすかというお金との関係性全体を示します。あなたの金運線は、単なる収入の多さではなく、財を引き寄せる磁力の強さを表しています。',
        'この線が薬指と小指の間のエリアに向かって伸びているのは、才能や個性を通じてお金を引き寄せることができることを意味します。好きなことや得意なことを仕事にするほど金運が上がるタイプです。',
        '線の途中にある財布の相（小さな四角い印）がある場合、贈り物・遺産・思わぬ臨時収入など予期しない収入が生涯に複数回訪れることを示しています。',
      ],
      advice:['毎月少額からでも投資を始めることが金運線を活性化させます','お金に対するネガティブな信念を手放すことで財運が開きます','「与えることで豊かになる」という循環を意識しましょう','家計簿でお金の流れを見える化することで金運アップします'],
    }
  },
  popular: {
    scores:[91,76,85,92,78,88,70,93,82,75],
    texts:[
      '人気線が鮮明に刻まれており、多くの人に好かれる天性の魅力を持っています。どんなコミュニティに入っても自然と中心的な存在になれる素質があります。',
      '太陽指方向へ伸びる人気線は、SNSや公の場での知名度アップを示す吉相です。発信活動を始めると一気に注目を集める可能性があります。',
      '人気線が明確に存在し、特に異性からの人気が高い相を示しています。存在感があり、自然と人が集まってくる磁力の強い人です。',
    ],
    deep:{
      title:'人気線・詳細鑑定',
      content:[
        '人気線は、その人が社会の中でどれほどの「引力」を持つかを示す線です。この線が強い人は意識せずとも人々を惹きつけ、自然とコミュニティの中心になっていきます。',
        'あなたの人気線の始点が生命線や感情線と近い位置にあることは、人気の源泉が「パフォーマンス」ではなく「真摯な人間性」にあることを示しています。ありのままのあなたが人を惹きつけています。',
        'このタイプの人気は息が長いのが特徴です。年齢を重ねるほどに「あの人は信頼できる」という形で評価が高まっていきます。特に30代後半から40代にかけて人気運が一段と花開きます。',
      ],
      advice:['自己紹介を磨く——自分の強みを30秒で語れるようにしましょう','笑顔と相手の名前を呼ぶことが人気運を大きく高めます','SNSや動画での定期的な発信が潜在的な人気を顕在化させます','「受け取り上手」になることでさらに人が寄ってきます'],
    }
  },
  charm: {
    scores:[88,75,93,80,92,70,85,77,90,82],
    texts:[
      'モテ線（魅惑線）が美しく伸びており、異性を惹きつける天然の色気と魅力を持っています。意識しなくても多くの人から好意を寄せられる相です。',
      'モテ線の存在は、外見だけでなく内面的な魅力——雰囲気・声・仕草——で人を惹きつける特別な個性を示しています。',
      'はっきりとしたモテ線は、生まれながらのフェロモンの強さを示しています。同性異性問わず「この人の傍にいたい」と思わせる不思議な磁力があります。',
    ],
    deep:{
      title:'モテ線・詳細鑑定',
      content:[
        'モテ線（魅惑線）は手相の中でも特に神秘的な線のひとつです。この線は恋愛的な魅力だけでなく、「人を惹きつける総合的な磁力」を表しています。',
        'あなたのモテ線は知能線から分かれて上方向に向かう形を示しており、知性と感性が融合した魅力を持つタイプです。頭が良くて思いやりがある——この組み合わせは異性に最も響く魅力の形です。',
        'この線がある人は「意識していない時ほどモテる」という特徴があります。頑張りすぎず自然体でいる時に最も輝きます。また年齢を重ねるほどにその魅力が深みを増すタイプです。',
      ],
      advice:['作り込まずに自然体でいることが最大のモテ戦略です','「聞き上手」を意識するとモテ線のエネルギーが倍増します','自分の好きな香りを持つことがフェロモンオーラを高めます','目線と笑顔に少し意識を向けるとモテ運が劇的に変わります'],
    }
  },
  devotion: {
    scores:[82,90,75,88,70,93,80,78,85,91],
    texts:[
      'つくし線が刻まれており、愛する人のためなら自分を犠牲にできる深い献身の心を持っています。その純粋な愛は必ず相手の心を動かします。',
      'つくし線の存在は、人のために尽くすことで自分も幸福を感じられる「give型」の愛情パターンを示しています。その姿勢が周囲に深い感動を与えます。',
      'つくし線が見られ、パートナーや大切な人への深い愛情と責任感を持つ人です。その献身が最終的に大きな愛情となって返ってくる幸運な相です。',
    ],
    deep:{
      title:'つくし線・詳細鑑定',
      content:[
        'つくし線は愛の深さと人への献身度を表す稀有な線です。この線を持つ人は、愛する人のために自分の時間・エネルギー・場合によっては夢をも差し出せる深い愛の器を持っています。',
        'これは美しい特質である反面、自分を後回しにしすぎると長期的には心が疲れてしまいます。あなたのつくし線は生命線と近い位置から始まっており、「自己犠牲」の傾向と「自己肯定感」のバランスを取ることが人生のテーマであることを示しています。',
        'このタイプが真に幸福になるためのカギは「自分を愛する」ことです。自分を大切にしながら相手を愛する——このバランスが取れた時に、つくし線は最もポジティブな形で輝きます。',
      ],
      advice:['「自分への時間」を週に1日は確保する習慣を作りましょう','NOを言える勇気があなたの魅力をさらに高めます','与えることと受け取ることのバランスを意識しましょう','自己肯定感を高める言葉を毎朝唱えてみてください'],
    }
  },
};

const PERSONALITIES = [
  '手相全体から読み取れるあなたの人間性は、深い共感力と強い意志を兼ね備えた稀有な存在です。表面上は穏やかで人当たりが良く、誰とでもすぐに打ち解けられる社交性を持ちながら、内側には揺るぎない芯と信念が宿っています。\n\nあなたの最大の魅力は「誠実さ」です。約束を守り、言葉と行動が一致している姿勢が、気づかないうちに多くの人からの信頼を勝ち取っています。繊細な感受性が芸術や美しいものへの高いセンスとなって現れており、創造的な活動においても才能を発揮できるでしょう。\n\n一方で完璧主義な面があり、自分に厳しくなりすぎることも。もう少し自分を許す余裕を持つと、さらにエネルギーが循環しやすくなります。',
  '手のひら全体の線のバランスから、あなたは知性と感性を高次元で統合した、非常に稀な手相の持ち主であることが分かります。論理的思考と直感、理性と感情——これらを自由自在に使い分けながら人生の複雑な問いに向き合う力があります。\n\nコミュニケーションにおいては、相手の言葉の裏にある感情を読み取る能力が突出しています。これがあなたを優れたリスナーにする一方、人の悩みを吸い込みすぎてしまう繊細さでもあります。\n\nあなたの人生は「人との縁」によって大きく動いていきます。日々の人とのつながりを大切にする姿勢が、運命そのものを変える力を持っています。',
  'あなたの手相は、強い生命力と豊かな創造性を持つ魂を示しています。物事の本質を見抜く直感力と、それを実現するための行動力が見事に共存しており、「夢想家」であると同時に「実行者」でもあるバランスの良い人格を形成しています。\n\n人間関係においては深く少数の関係を好み、表面的な付き合いよりも本音で語り合える絆を大切にします。その深い愛着心と義理堅さから、一度仲間として認めた人とは生涯にわたる絆を結ぶことができます。\n\n「自分にとっての正しさ」に従って行動するスタイルが、あなたの人生を独自の彩りで満たす源になっています。',
];

const FUTURES = [
  { period:'〜6ヶ月', label:'直近', title:'新しい縁と出会い', text:'今後半年以内に、あなたの人生に大きな影響を与える人物との出会いが予感されます。職場・趣味・SNSなど意外な場所でのご縁を大切に。今まで縁がなかった分野の人との交流が新しい道を開くカギになりそうです。' },
  { period:'〜1年', label:'1年以内', title:'転機とチャンスの到来', text:'大きな変化を求めていた思いが現実に動き出す時期です。転職・引越し・新しいプロジェクトなど「踏み出す勇気」が求められる局面が訪れます。迷いがあっても直感を信じて一歩踏み出すと、運命の歯車が回り始めます。' },
  { period:'1〜3年', label:'中期', title:'実力が評価される時期', text:'これまで積み上げてきた努力と実力が正当に評価される時期が訪れます。自分の強みを活かした場所での活躍が期待でき、経済的にも精神的にも充実した日々が待っています。着実に進んだ先に大きな花が咲きます。' },
  { period:'3〜5年', label:'長期', title:'愛と絆の深まり', text:'人生における愛——恋愛・家族・友情——がより深く豊かな形へと進化する時期です。大切なパートナーとの絆の深まり、もしくは真の意味での生涯のパートナーとの出会いが暗示されています。愛を与え受け取ることへの恐れを手放しましょう。' },
];

// ===================== 人生詳細鑑定データ =====================
const LIFE_READINGS = [
  {
    id:'health', icon:'🏥', title:'健康・病気の注意点', badge:'健康運',
    variations:[
      { headline:'消化器系・胃腸に注意を', text:'生命線の刻みとその弧の形から、ストレスが消化器系に出やすい体質であることが読み取れます。特に仕事が忙しい時期は胃もたれ・胃痛・腸の不調が起こりやすいでしょう。規則正しい食事と睡眠が最大の予防策。心が安定すると体も整う体質です。また、季節の変わり目（春・秋）は免疫力が低下しやすいため、この時期に意識してメンテナンスを行うことをおすすめします。', advice:['腹八分目と温かい食事を意識しましょう','ストレスを溜め込まず週1回は発散の時間を','睡眠7時間を目標に就寝リズムを整えましょう','春・秋の変わり目には人間ドックや検診の予約を'] },
      { headline:'疲れが出やすい神経・目のケアを', text:'手相の線の細さと知能線の形から、神経系の繊細さが読み取れます。脳疲労・眼精疲労・頭痛が現れやすいタイプ。デジタル機器の使い過ぎが大きなリスクになります。定期的なデジタルデトックスと、緑の多い環境での休息があなたの生命力をリチャージします。大病はしにくい体質ですが、慢性的な疲れが積み重なると自律神経の乱れとして表れやすいので要注意です。', advice:['スマホ・PC使用後は20-20-20ルール（20分ごとに20秒遠くを見る）を','週に1回は自然の中を歩く時間を確保しましょう','入浴は38〜40度のぬるめ湯に15分浸かることで自律神経が整います','ビタミンB群・マグネシウムの補給が神経ケアに効果的'] },
      { headline:'循環器・血流の維持が長寿のカギ', text:'生命線の力強さから長寿の相が読み取れますが、同時に手相全体の色艶（血色）から、血流・循環器系のメンテナンスが重要であることがわかります。特に40代以降は血圧・コレステロールの管理が鍵。若いうちから有酸素運動の習慣をつけることで、老後まで活動的でいられる可能性が大幅に高まります。禁煙・節酒が最も効果的な投資です。', advice:['週3回30分のウォーキングが最も効果的な循環器ケアです','塩分を控えた和食中心の食事が体質に合っています','40歳からは血圧・血糖・コレステロールを年1回チェック','禁煙・節酒は老後の活力を守る最大の保険です'] },
    ]
  },
  {
    id:'money_now', icon:'💴', title:'現在の金運', badge:'今の金運',
    variations:[
      { headline:'収入は安定。支出管理が鍵の時期', text:'金運線と運命線の交わりから、現時点での収入は比較的安定している相が出ています。しかし「入ってくるが出ていくのも早い」サイクルにある可能性も。衝動買い・人への奢り・不必要なサブスクリプションなど小さな支出の積み重ねが資産形成を妨げているかもしれません。今すぐ家計の「見える化」に取り組むことで、3ヶ月以内に手元資金が大幅に改善します。', advice:['家計簿アプリで支出を3カテゴリに分けて見える化しましょう','固定費（サブスク・保険）の見直しから始めると効果大','「欲しいもの」を買う前に72時間待つルールを試してみて','少額（月1000円〜）でも積立投資を今すぐ始めましょう'] },
      { headline:'金運上昇中。今が投資・貯蓄の好機', text:'太陽線と金運線が重なるように伸びており、現在は金運が上昇局面にある吉相です。行動したことが結果につながりやすい時期なので、副業・投資・スキルアップへの投資を積極的に検討しましょう。ただし投機的なギャンブルや「すぐ儲かる話」には要注意。堅実な資産形成が、この時期に取り組むと長く実を結ぶ方法です。', advice:['新NISA・iDeCoへの加入を今すぐ検討しましょう','副業・フリーランス案件を1件挑戦してみる価値ありです','「お金の本」を月1冊読む習慣が金運を高めます','高額商品・投資話は必ず第三者に相談してから判断を'] },
      { headline:'金運の転換期。準備が実りを決める', text:'運命線の形から、現在は金運の「種まき期」にあることが読み取れます。今すぐ大きな収入は見込みにくいですが、今の時期に積み重ねたスキル・知識・人脈が2〜3年後に大きな経済的実りをもたらします。焦らず地道に実力をつけることが最善策。節約よりも「稼ぐ力の向上」に意識を向けると吉。', advice:['市場価値を高めるスキルに月収の5%を投資しましょう','副業や複業で収入の柱を増やす準備を始めましょう','節約と同時に収入アップの方法を具体的に考えてみて','信頼できる人との共同プロジェクトが思わぬ実りを生みます'] },
    ]
  },
  {
    id:'windfall', icon:'💎', title:'大金を手にする可能性', badge:'臨時収入',
    variations:[
      { headline:'才能を活かした収入で大金の予感', text:'太陽線と金運線が強く出ている場合、「才能・個性・創造力」を活かすことで普通の労働収入を超えた大きな収入を得られる可能性があります。特にコンテンツ作成・デザイン・教育・エンターテインメント分野での収益化が鍵。一発逆転ではなく、得意なことを発信し続けることで、気づけば大きな額になっているタイプの収入が期待できます。可能性は「高め」の相です。', advice:['自分の得意なことをSNSや動画で発信してみましょう','知識や経験を商品化（講座・電子書籍等）する検討を','コンテストや公募への挑戦が思わぬ賞金・機会を生みます','今の仕事のスキルを副業に活かせないか考えてみて'] },
      { headline:'人との縁が大金を引き寄せるタイプ', text:'感情線と運命線の組み合わせから、「人との信頼関係」がお金を引き寄せるタイプであることが分かります。紹介ビジネス・コミュニティ運営・共同事業など、人との絆を活かした収益化で大きな成果が出やすい相です。また、誠実な仕事ぶりが評価されての昇給・出世・大型案件の受注という形で大金が入ってくる可能性もあります。確率は「中程度」ですが、行動次第で高まります。', advice:['既存の人間関係を大切にすることが最高の投資です','ビジネスの相談は信頼できる人に積極的に持ちかけを','人に頼み事をする勇気が新たな機会を生み出します','地域や業界のコミュニティへの参加で縁が広がります'] },
      { headline:'遺産・保険・臨時収入の可能性あり', text:'手相の特定の線の交差から、生涯のどこかで予期しない収入（遺産・保険金・臨時ボーナス・宝くじ的な幸運）が訪れる可能性が示されています。時期としては35〜50歳の間に特にこうした「棚ぼた」的な幸運が訪れやすい相です。ただし、それに頼ることなく日々の努力と貯蓄を続けることが大前提。幸運は準備した人のところへ来ます。', advice:['生命保険・医療保険の内容を今一度確認しておきましょう','緊急資金として生活費3〜6ヶ月分を貯蓄しておくと安心','宝くじは遊び程度に。メインの財源は自分の力で作りましょう','思いがけない依頼には「まず話を聞く」姿勢が大切です'] },
    ]
  },
  {
    id:'love', icon:'💑', title:'現在の恋愛傾向', badge:'恋愛運',
    variations:[
      { headline:'新しい愛の予感。心を開く時期', text:'感情線の形とモテ線の存在から、現在は新しい愛の縁が引き寄せられやすい時期であることが分かります。ただし、あなたは内向きな思考が強いため「待っているだけ」では縁を逃しがちです。自分から少し心を開いて行動するだけで、相手の方から動いてくれる展開が訪れます。恋愛に対する理想が高い傾向がありますが、今は完璧を求めず「まず会ってみる」を心がけると吉です。', advice:['新しい出会いの場に月に1〜2回は顔を出しましょう','LINE・SNSでの返信レスポンスを早めてみましょう','「理想の恋人像」を紙に書いて明確にしておくと引き寄せ力が上がります','外見・内面のセルフケアに今すぐ投資を'] },
      { headline:'深まる絆。関係性が次のステージへ', text:'感情線が深く刻まれており、現在の関係が「表面的な付き合い」から「深い絆」へと進化しようとしている時期であることを示しています。パートナーがいる方は、より深い対話や共通の体験を重ねることで関係が一段と豊かになります。パートナーがいない方も、既存の縁（友人・知人）の中に将来の恋人候補がいる可能性が高い相です。', advice:['パートナーとの「ちゃんとした対話の時間」を週1回作りましょう','相手への感謝を言葉にして伝える習慣を始めましょう','共通の趣味・体験（旅行・料理・スポーツ）が絆を深めます','一人の時間も大切に。依存しすぎない自立した愛が理想です'] },
      { headline:'恋愛より自分磨きが先の時期', text:'現在の運命線と感情線の関係から、今は恋愛よりも自己成長に集中すべき時期であることが読み取れます。焦って恋愛を求めるより、仕事・趣味・健康に打ち込むことで自然と魅力が増し、良質な恋愛が向こうからやってきます。「自分が楽しんでいる姿」が最大のモテ要素。半年〜1年後には素敵な出会いの波が来る予感です。', advice:['好きなことに没頭する時間を増やしましょう','1人でできる趣味（語学・運動・料理）を始めると自信がつきます','恋愛アプリは「縁」として使いつつ、執着しすぎないこと','自分を大切にできる人が素敵な恋愛も引き寄せます'] },
    ]
  },
  {
    id:'retirement', icon:'🏡', title:'老後のお金・生活の安定', badge:'老後運',
    variations:[
      { headline:'早めの行動で老後は安泰な相', text:'生命線の力強さと運命線の走り方から、老後に経済的な危機に陥る可能性は低い吉相です。ただし「自然と積み上がる」タイプではなく、今から意識的に準備した人が安定を手に入れます。iDeCo・NISA・積立型保険などを30代・40代のうちに始めた人は、65歳時点でゆとりある生活を送れる可能性が大幅に高まります。今すぐ始めることが最大の老後対策です。', advice:['iDeCoかNISAを今月中に口座開設しましょう','老後に必要な資金（目安：退職前年収×15〜20年分）を試算してみて','退職後も「働き続けられるスキル」を今から身につけましょう','健康維持が最大の老後リスク回避策です。今すぐ運動習慣を'] },
      { headline:'人の縁が老後の安心を作る相', text:'手相全体から、晩年を豊かにするのは「お金」よりも「人との絆」であることが読み取れます。家族・友人・コミュニティとのつながりを大切にしてきた人ほど、老後に精神的・物質的なサポートを受けやすい相です。孤立を防ぐためのコミュニティ参加と、若い世代との交流が晩年の豊かさを左右します。資産形成と並行して、人間関係の貯金も今から始めましょう。', advice:['地域コミュニティ・趣味サークルへの参加を今から始めましょう','親族との関係を丁寧に維持することが晩年の安心につながります','趣味を持つことが生きがいとなり老後うつの予防になります','資産形成と「人的資産」の両方を積み上げていきましょう'] },
      { headline:'仕事が生きがいになる充実した老後', text:'運命線が長く伸びており、定年後も何らかの形で社会に貢献し続ける活動的な晩年が示されています。「仕事を辞めたら終わり」ではなく、退職後も技術・経験・ネットワークを活かして活躍できる可能性が高い相です。今から「老後も需要がある自分」を作ることが最大の老後対策。年金だけに頼らない収入源を複数確保することが鍵です。', advice:['退職後も通用するスキル・資格への投資を今から始めましょう','フリーランスや顧問業として関われる分野を今から開拓を','趣味を収入に変える可能性を模索しておくと老後が楽しくなります','健康が最大の資本。今の生活習慣の見直しが老後を左右します'] },
    ]
  },
  {
    id:'career', icon:'💼', title:'仕事・キャリアの行方', badge:'仕事運',
    variations:[
      { headline:'実力が認められ評価が上がる時期', text:'運命線が力強く伸び、太陽線が存在することから、今後のキャリアで「実力が正当に評価される」展開が期待できます。自分の得意分野に集中して実績を積み上げることが最短の出世ルート。転職・独立・昇進いずれのルートでも、今後2〜3年の間に大きなポジションアップのチャンスが訪れる予感です。周囲への貢献と自己アピールのバランスが成功の鍵。', advice:['自分の「得意分野×需要のある市場」の交差点を探しましょう','実績を数字で語れるよう、成果を記録する習慣をつけましょう','社内外の信頼できる人に積極的に相談を持ちかけましょう','昇進・転職は「タイミング」より「準備」が大切です'] },
      { headline:'転換期。新しいフィールドで才能開花', text:'運命線の途中から新しい線が分岐している相は、キャリアの大きな転換が訪れることを示しています。これまでの経験が全く別の分野で活きる可能性があります。「今の仕事に疑問を感じている」なら、それは転換のサインです。副業・異業種交流・学び直しなどで新しいフィールドのドアを試してみる時期。恐れずに行動した人が5年後に大きく飛躍します。', advice:['興味のある分野の勉強を「まず3ヶ月」続けてみましょう','副業や趣味として新分野に触れることで適性が見えてきます','異業種の人との交流を意識的に増やしていきましょう','キャリアコンサルタントへの相談が転換期の指針になります'] },
      { headline:'着実に実力を蓄える「土台作り」期', text:'現在は大きな昇進や収入アップよりも、スキル・知識・人脈を蓄積する「土台作り」の時期であることが手相から読み取れます。地道に見えても今積み上げているものが、3〜5年後に一気に花開く基盤になります。焦らず自分の専門性を深める行動が最善策。副業・資格取得・社内外での人脈構築を今から意識的に進めましょう。', advice:['専門書を月1冊読む習慣が5年後の実力差を生みます','業界の勉強会・セミナーに月1〜2回参加しましょう','社内の違うチームや部署の人と積極的に関わりましょう','資格・認定証は市場価値を可視化する強力な武器です'] },
    ]
  },
  {
    id:'job_type', icon:'🎯', title:'向いている職種・才能分野', badge:'適職',
    variations:[
      { headline:'対人力を活かす職種で輝く', text:'感情線と知能線のバランスから、人と接する仕事で最大限の才能を発揮できるタイプであることが分かります。具体的には：カウンセラー・教師・医療・福祉・営業・HR・コーチング・コンサルタント・接客・PRなどの職種で高いパフォーマンスを発揮します。感情的知性（EQ）が高く、相手の気持ちを察する力が天性の武器。人の役に立てる仕事ほど、金銭的報酬も精神的報酬も最大化されます。', advice:['今の仕事で「人の役に立てた」瞬間を大切にしましょう','対人スキルを磨くコーチングや傾聴の学習がキャリアを高めます','コミュニティマネージャーやファシリテーターの経験を積んでみては','人のために動いた分、しっかり報酬を求める勇気も持ちましょう'] },
      { headline:'創造力・表現力を活かすクリエイティブ職が適職', text:'太陽線の存在と知能線の下方向のカーブから、創造力・審美眼・独創性が卓越したタイプであることが分かります。具体的には：デザイナー・映像クリエイター・ライター・ミュージシャン・料理人・建築・写真・ファッション・広告などの分野で本来の才能が開花します。「作ること」「表現すること」をお金にできれば最高の人生に。副業からでも始めることをおすすめします。', advice:['作品を作ったら必ずSNSやポートフォリオで発信しましょう','クリエイティブな仕事への転職・副業を具体的に調べてみて','美術館・映画・音楽フェスなどインプットの場に定期的に行きましょう','「好きなこと」と「得意なこと」の交差点が最高の仕事になります'] },
      { headline:'分析力・論理力を活かす専門職・研究職', text:'知能線が長く水平方向に伸びており、高い論理的思考力と分析力を持つタイプであることが読み取れます。具体的には：エンジニア・研究者・データアナリスト・法律家・会計士・経営コンサルタント・医師・薬剤師・経済学者・金融など、専門知識と論理力を求める職種で頭角を現します。資格・専門知識への投資が最高のキャリア投資になる相です。', advice:['専門資格・認定証の取得を来年度の目標に設定しましょう','最新の論文・業界動向を継続的にフォローする習慣を','異分野の知識を組み合わせることで独自の強みが生まれます','SpecialistよりGeneralist寄りの視点も取り入れることで差別化を'] },
    ]
  },
  {
    id:'marriage', icon:'👪', title:'結婚・家族運', badge:'家族運',
    variations:[
      { headline:'誠実な愛がやがて家庭を築く', text:'結婚線と感情線の位置関係から、あなたは「本物の絆」を大切にするタイプであることが分かります。表面的な付き合いより、深く信頼し合えるパートナーとの関係を望んでいます。その誠実さと真剣さが、やがて安定した家庭運として実を結ぶ相です。結婚の時期としては、仕事が落ち着きはじめる30代前後か、大きな転機の前後にご縁が訪れる可能性が高いでしょう。家族ができると運気全体が上昇するタイプです。', advice:['パートナーへの感謝を言葉で伝える練習をしましょう','「一緒に成長できる関係か」を関係の基準にしましょう','自分の家庭に対するビジョンを明確に言語化してみて','入籍・同棲などの節目は直感を大切に判断しましょう'] },
      { headline:'子縁に恵まれた家庭的な幸運の相', text:'手相の線全体のバランスから、子どもや家族との縁が強い相が出ています。お子さんがいる方は、その子たちがあなたの人生に大きな幸運をもたらす存在であることを示しています。まだの方は、子縁が将来の大きな喜びとなる可能性が示唆されています。家族のために頑張る時のあなたは、通常の何倍もの力を発揮できるタイプ。家族の存在が最大の原動力になります。', advice:['家族との時間を「最優先事項」としてスケジュールに入れましょう','子どもへの教育投資は最高の財産となって返ってきます','パートナーへの思いやりを言葉と行動両方で示しましょう','家族の誕生日・記念日を大切にすると縁が深まります'] },
      { headline:'自立した二人が作る対等な関係が理想', text:'手相全体から、あなたは依存関係よりも「自立した二人の対等なパートナーシップ」が最も輝く形であることが分かります。お互いの個を尊重しながら、共通の目標や価値観を持てるパートナーとの関係が最高の家族運をもたらします。束縛や依存は逆効果。自分が自立し、充実した人生を歩んでいる時ほど、理想のパートナーが引き寄せられてくる相です。', advice:['自分の趣味・仕事・友人関係を豊かにすることが最善のパートナー引き寄せ術','相手に求める前に自分が理想のパートナーになることを意識して','価値観・将来の方向性を早めにオープンに話し合いましょう','二人でできる目標（旅行・貯蓄・趣味など）を設定すると絆が深まります'] },
    ]
  },
  {
    id:'luck_timing', icon:'⚡', title:'人生の転機・チャンスの波', badge:'転機予測',
    variations:[
      { headline:'2〜3年以内に大きな波が来る', text:'運命線の走り方と生命線のリズムから、今後2〜3年の間に人生を大きく変える転機が訪れることが予感されます。その波は「チャンス」の形で訪れますが、準備がなければ気づかずに過ぎ去ってしまいます。今のうちに「もし大きな変化が来たらどう動くか」のビジョンを持っておくことが重要。転機の際に最も大切なのは「行動のスピード」です。機会が見えたら素早く動く習慣を今から身につけましょう。', advice:['「もし転機が来たらどうするか」を今から具体的に考えておきましょう','スキルアップと人脈づくりで転機を生かす準備を','直感で「これだ」と感じた機会は大切に扱いましょう','転機は怖くなく、次のステージへの招待状です'] },
      { headline:'今まさに転機の真っ只中にある', text:'手相の線の乱れと新しい枝線の出現から、あなたは今まさに人生の転換期にある可能性があります。不安定さや迷いを感じているなら、それは変化の痛みではなく、成長の痛みです。このタイミングで下す決断は、5年後のあなたを大きく左右します。焦らず深く考えながらも、決めたことへの行動は迷いなく。人生で最も大切な決断が近づいています。', advice:['今の状況を紙に書き出して、選択肢を整理しましょう','信頼できる人に正直に相談することで道が開けます','どんな決断をしても「それが正解」と思える強さを持ちましょう','この転機を振り返った時に「あの経験があったから」と言える決断を'] },
      { headline:'5年後に大きな飛躍が待っている', text:'運命線が中間から急激に太く深くなる相が読み取れ、5年後前後に大きな飛躍と充実期が訪れることを示しています。今は種まき期と割り切り、焦らず着実に土台を積み上げることが最善策。人脈・スキル・実績・健康——これらを今から意識して積み上げた人が、5年後の花開きのタイミングで最大限の結果を手にします。', advice:['5年後のなりたい自分を具体的にイメージして書き出しましょう','今すぐできる「5年後への投資」を1つ始めましょう','焦りを感じたら「今は根を張る時期」と自分に言い聞かせましょう','5年後の自分が「よくやった」と言ってくれる行動を今日から'] },
    ]
  },
  {
    id:'relationships', icon:'🤝', title:'人間関係・対人運', badge:'対人運',
    variations:[
      { headline:'信頼できる人脈が人生を動かす', text:'感情線と運命線の関係から、あなたの人生は「誰と出会うか」によって大きく動いていくことが分かります。表面的な知り合いを増やすより、深い信頼関係を少数の人と育てることがあなたにとっての最善の人脈形成です。今の生活の中に、将来の大きな縁の種がすでにあります。既存のつながりを大切に丁寧に育てることで、5年後に「あの人のおかげで」という人物が必ず現れます。', advice:['毎月1人、既存の友人・知人に連絡を取りましょう','人の成功を心から喜べる「応援力」がさらに人脈を広げます','紹介してもらった縁は必ず大切にしましょう','SNSより直接会って話す時間を増やすと縁が深まります'] },
      { headline:'与える姿勢が豊かな人間関係を作る', text:'つくし線と感情線の組み合わせから、「与える側」に回ることで豊かな人間関係が生まれるタイプであることが分かります。ただし、与えすぎて消耗するリスクも。大切なのは「気持ちよく与えられる量」を把握すること。頼まれたことに感謝して応じながら、無理な場合は「今回は難しい」と伝えられる勇気も必要です。Give & Takeのバランスを意識した関係が長続きします。', advice:['「断ること」も愛のある人間関係の維持に必要です','毎日1つ、誰かへの小さな親切を実践してみましょう','感謝は心の中だけでなく言葉や行動でも表しましょう','自分の限界を相手に伝えることも大切なコミュニケーションです'] },
      { headline:'コミュニティの中心になる大きな社会性', text:'人気線と感情線の力強さから、集団の中で自然と中心的な役割を担う社会性の高さが読み取れます。リーダー・まとめ役・相談役——こうした立場があなたの才能を最大限に引き出します。職場・地域・趣味コミュニティなど、複数の「居場所」を持つことが運気全体を高めます。一つのコミュニティに固執せず、多様な場に顔を出すことで人脈が指数関数的に広がります。', advice:['コミュニティのイベント企画・運営に関わってみましょう','職場・趣味・地域の3つの居場所を持つことを目標に','新しいコミュニティへの参加を年に2〜3回試みましょう','人の名前と特徴を覚える努力が信頼関係の基盤になります'] },
    ]
  },
];

let lifeReadingData = {}; // 各カテゴリで選ばれたvariation

// ===================== STATE =====================
let imgSrc = null;
let lineVis = {};
let scores = {};
let readingTexts = {};
let currentTab = 'Lines';

// ===================== INIT =====================
document.addEventListener('DOMContentLoaded', () => {
  setupUpload();
  setupTabs();
});

// ===================== UPLOAD =====================
function setupUpload() {
  const fileInput = document.getElementById('fileInput');
  const uploadBtn = document.getElementById('uploadBtn');
  const dropZoneBtn = document.getElementById('dropZoneBtn');
  const screenUpload = document.getElementById('screenUpload');
  const dropOverlay = document.getElementById('dropOverlay');

  uploadBtn.addEventListener('click', () => fileInput.click());
  dropZoneBtn.addEventListener('click', () => fileInput.click());
  fileInput.addEventListener('change', e => { if (e.target.files[0]) loadFile(e.target.files[0]); });

  screenUpload.addEventListener('dragover', e => { e.preventDefault(); dropOverlay.classList.add('active'); });
  screenUpload.addEventListener('dragleave', () => dropOverlay.classList.remove('active'));
  screenUpload.addEventListener('drop', e => {
    e.preventDefault();
    dropOverlay.classList.remove('active');
    if (e.dataTransfer.files[0]) loadFile(e.dataTransfer.files[0]);
  });

  document.getElementById('retakeBtn').addEventListener('click', resetToUpload);
}

async function loadFile(file) {
  try {
    // MIME型が空でも拡張子で画像か判断
    const ext = (file.name || '').split('.').pop().toLowerCase();
    const knownImg = /^(jpe?g|png|webp|gif|bmp|avif|heic|heif|tiff?)$/.test(ext);
    if (!file.type.startsWith('image/') && !knownImg) return;

    const isHeic = file.type === 'image/heic' || file.type === 'image/heif'
      || /\.(heic|heif)$/i.test(file.name || '');

    let objUrl = URL.createObjectURL(file);

    if (isHeic) {
      // まずブラウザのネイティブ HEIC 対応を試みる（Safari はネイティブで対応）
      const canLoadNative = await new Promise(resolve => {
        const t = new Image();
        const timer = setTimeout(() => resolve(false), 5000);
        t.onload  = () => { clearTimeout(timer); resolve(true); };
        t.onerror = () => { clearTimeout(timer); resolve(false); };
        t.src = objUrl;
      });

      if (!canLoadNative) {
        // ネイティブ非対応 → heic2any で JPEG 変換
        URL.revokeObjectURL(objUrl);
        if (typeof heic2any === 'undefined') {
          alert('ページを再読み込みしてからお試しください。');
          return;
        }
        const converted = await heic2any({ blob: file, toType: 'image/jpeg', quality: 0.88 });
        const blob = Array.isArray(converted) ? converted[0] : converted;
        objUrl = URL.createObjectURL(blob);
      }
    }

    if (imgSrc && imgSrc.startsWith('blob:')) URL.revokeObjectURL(imgSrc);
    imgSrc = objUrl;
    runAnalysis();

  } catch(err) {
    console.error('loadFile error:', err);
    alert('写真の読み込みに失敗しました。\n別の写真か、JPEGに変換してお試しください。\nエラー: ' + (err.message || String(err)));
  }
}

// ===================== ANALYSIS =====================
function runAnalysis() {
  showLoading();
  generateData();

  const steps = [
    '手のひらの輪郭を検出中...',
    '主要な掌紋を認識中...',
    '生命線・感情線を解析中...',
    '特殊線（モテ線・金運線）を探索中...',
    '人格パターンを分析中...',
    '運命の流れを読み解いています...',
  ];

  const stepEl = document.getElementById('loadingStep');
  const progressEl = document.getElementById('progressBar');
  let i = 0;

  const tick = () => {
    if (i < steps.length) {
      stepEl.textContent = steps[i];
      progressEl.style.width = ((i + 1) / steps.length * 100) + '%';
      i++;
      setTimeout(tick, 500);
    } else {
      setTimeout(() => {
        hideLoading();
        showResult();
      }, 400);
    }
  };
  tick();
}

function generateData() {
  PALM_LINES.forEach(l => {
    lineVis[l.id] = true;
    const d = READINGS[l.id];
    scores[l.id] = d.scores[Math.floor(Math.random() * d.scores.length)];
    readingTexts[l.id] = d.texts[Math.floor(Math.random() * d.texts.length)];
  });
  // 人生詳細鑑定データの生成
  LIFE_READINGS.forEach(cat => {
    lifeReadingData[cat.id] = cat.variations[Math.floor(Math.random() * cat.variations.length)];
  });
}

// ===================== SHOW RESULT =====================
function showResult() {
  // Transition screens
  const uploadScreen = document.getElementById('screenUpload');
  const resultScreen = document.getElementById('screenResult');
  uploadScreen.classList.add('slide-out');
  setTimeout(() => { uploadScreen.classList.remove('active', 'slide-out'); }, 350);
  resultScreen.classList.add('active');

  // Show tab bar
  const tabBar = document.getElementById('tabBar');
  tabBar.style.display = 'flex';

  // Show top bar actions
  document.getElementById('retakeBtn').style.opacity = '1';
  document.getElementById('retakeBtn').style.pointerEvents = 'auto';

  // Draw canvas
  drawCanvas();

  // Build score cards
  buildScores();

  // Build reading list
  buildReadings();

  // Build personality
  buildPersonality();

  // Build future
  buildFuture();

  // Build detail options
  buildDetailOptions();

  // Set tab to lines
  switchTab('Lines');

  // Animate score bars after render
  setTimeout(() => {
    document.querySelectorAll('.score-fill').forEach(el => {
      el.style.width = el.dataset.w + '%';
    });
  }, 400);
}

// ===================== MEDIAPIPE =====================
let handsInstance = null;
let landmarkPending = null;
let detectedLandmarkPaths = null;

async function getHandsInstance() {
  if (handsInstance) return handsInstance;
  const h = new Hands({ locateFile: f => `https://cdn.jsdelivr.net/npm/@mediapipe/hands/${f}` });
  h.setOptions({ maxNumHands:1, modelComplexity:1, minDetectionConfidence:0.5, minTrackingConfidence:0.5 });
  h.onResults(r => {
    if (landmarkPending) {
      landmarkPending(r.multiHandLandmarks?.length ? r.multiHandLandmarks[0] : null);
      landmarkPending = null;
    }
  });
  await h.initialize();
  handsInstance = h;
  return h;
}

async function detectLandmarks(imgEl) {
  try {
    const h = await getHandsInstance();
    return await new Promise(resolve => { landmarkPending = resolve; h.send({ image: imgEl }); });
  } catch(e) { return null; }
}

// ===================== IMAGE ANALYSIS =====================
function lerp(a, b, t) { return { x: a.x+(b.x-a.x)*t, y: a.y+(b.y-a.y)*t }; }

// グレースケール変換
function getGrayData(canvas) {
  const {width:W, height:H} = canvas;
  const px = canvas.getContext('2d').getImageData(0,0,W,H).data;
  const g = new Float32Array(W*H);
  for (let i=0;i<g.length;i++) g[i]=0.299*px[i*4]+0.587*px[i*4+1]+0.114*px[i*4+2];
  return g;
}

// 局所コントラストマップ: 周囲の肌より「どれだけ暗いか」= シワらしさ
// → 暗い背景は周囲も暗いのでスコア低。シワは周囲の肌より暗くスコア高。
function getCreaseMap(canvas) {
  const {width:W, height:H} = canvas;
  const g = getGrayData(canvas);
  // ブラー半径: 手のひら全体の約2%（シワ幅の数倍）
  const R = Math.max(8, Math.round(Math.min(W,H)*0.025));
  // セパラブル ボックスブラーで局所平均を計算（O(WH)）
  const tmp = new Float32Array(W*H);
  const blr = new Float32Array(W*H);
  // 水平方向
  for (let y=0;y<H;y++){
    let s=0, lo=0, hi=0;
    for (let x=0;x<W;x++){
      while(hi<=Math.min(x+R,W-1)){ s+=g[y*W+hi]; hi++; }
      while(lo<Math.max(0,x-R)){ s-=g[y*W+lo]; lo++; }
      tmp[y*W+x]=s/(hi-lo);
    }
  }
  // 垂直方向
  for (let x=0;x<W;x++){
    let s=0, lo=0, hi=0;
    for (let y=0;y<H;y++){
      while(hi<=Math.min(y+R,H-1)){ s+=tmp[hi*W+x]; hi++; }
      while(lo<Math.max(0,y-R)){ s-=tmp[lo*W+x]; lo++; }
      blr[y*W+x]=s/(hi-lo);
    }
  }
  // シワマップ: 局所平均 − 自身の輝度（暗い＝正の値）
  const c = new Float32Array(W*H);
  for (let i=0;i<W*H;i++) c[i]=Math.max(0, blr[i]-g[i]);
  return c;
}

// 指定エリアで「シワらしさが最も高い行」のY座標
function creasiestRow(c, W, H, x0, x1, y0, y1) {
  x0=Math.max(0,~~x0); x1=Math.min(W,~~x1);
  y0=Math.max(0,~~y0); y1=Math.min(H,~~y1);
  if (y0>=y1||x0>=x1) return ~~((y0+y1)/2);
  let bestY=~~((y0+y1)/2), best=-1;
  for (let py=y0;py<y1;py++){
    let s=0,n=0;
    for (let px=x0;px<x1;px++){s+=c[py*W+px];n++;}
    const v=s/n; if(v>best){best=v;bestY=py;}
  }
  return bestY;
}

// 指定エリアで「シワらしさが最も高い列」のX座標
function creasiestCol(c, W, H, x0, x1, y0, y1) {
  x0=Math.max(0,~~x0); x1=Math.min(W,~~x1);
  y0=Math.max(0,~~y0); y1=Math.min(H,~~y1);
  if (y0>=y1||x0>=x1) return ~~((x0+x1)/2);
  let bestX=~~((x0+x1)/2), best=-1;
  for (let px=x0;px<x1;px++){
    let s=0,n=0;
    for (let py=y0;py<y1;py++){s+=c[py*W+px];n++;}
    const v=s/n; if(v>best){best=v;bestX=px;}
  }
  return bestX;
}

// ===================== 手相線パス計算 =====================
function calcCreasePaths(lm, canvas) {
  const W=canvas.width, H=canvas.height;
  const p=lm.map(l=>({x:l.x*W, y:l.y*H}));
  // 局所コントラストマップ（背景の影に惑わされずシワを検出）
  const c=getCreaseMap(canvas);
  const lr=lerp;

  // ── 手全体のバウンディングボックス（全21点から厳密算出）──
  const allX=p.map(pt=>pt.x), allY=p.map(pt=>pt.y);
  const hMinX=Math.min(...allX), hMaxX=Math.max(...allX);
  const hMinY=Math.min(...allY), hMaxY=Math.max(...allY);

  // 座標を手の範囲内にクランプ
  const cx2 = v=>Math.max(hMinX, Math.min(hMaxX, v));
  const cy2 = v=>Math.max(hMinY, Math.min(hMaxY, v));

  // 手の基本寸法
  const fingerBaseY = Math.min(p[5].y,p[9].y,p[13].y,p[17].y);
  const wristY      = p[0].y;
  const palmH       = Math.abs(wristY - fingerBaseY);
  const centerX     = p[9].x;

  const flip       = p[5].x > p[17].x;
  const thumbSideX = flip ? Math.max(p[1].x,p[2].x) : Math.min(p[1].x,p[2].x);
  const pinkySideX = p[17].x;
  const xLeft      = Math.min(thumbSideX, pinkySideX);
  const xRight     = Math.max(thumbSideX, pinkySideX);
  const palmW      = xRight - xLeft;

  // ── シワスナップ: 解剖学的位置から searchR 以内でシワらしさ最大点を探す ──
  // 局所コントラストマップを使うので背景に引っ張られない
  function snapX(baseX, baseY, searchR) {
    const x0=Math.max(hMinX, baseX-searchR), x1=Math.min(hMaxX, baseX+searchR);
    const y0=Math.max(hMinY, baseY-palmH*0.05), y1=Math.min(hMaxY, baseY+palmH*0.05);
    if (x0>=x1||y0>=y1) return cx2(baseX);
    return cx2(creasiestCol(c, W, H, x0, x1, y0, y1));
  }
  function snapY(baseX, baseY, searchR) {
    const x0=Math.max(hMinX, baseX-palmW*0.06), x1=Math.min(hMaxX, baseX+palmW*0.06);
    const y0=Math.max(hMinY, baseY-searchR), y1=Math.min(hMaxY, baseY+searchR);
    if (x0>=x1||y0>=y1) return cy2(baseY);
    return cy2(creasiestRow(c, W, H, x0, x1, y0, y1));
  }

  // ─────────────────────────────────────────────
  // 【生命線】親指の付け根を囲むC字カーブ（二次ベジェ）
  // ─────────────────────────────────────────────
  const lifeP0 = {x:cx2(lr(p[2],p[5],0.52).x), y:cy2(lr(p[2],p[5],0.52).y)};
  const lifeP2 = {x:cx2(lr(p[0],p[1],0.32).x), y:cy2(lr(p[0],p[1],0.32).y)};
  const lifeCtrlX = cx2(thumbSideX + (flip?-1:1)*palmW*0.14);
  const lifeCtrlY = cy2((lifeP0.y+lifeP2.y)*0.5);
  const lifeRaw = [];
  for (let i=0;i<10;i++){
    const t=i/9, mt=1-t;
    const bx=mt*mt*lifeP0.x + 2*mt*t*lifeCtrlX + t*t*lifeP2.x;
    const by=mt*mt*lifeP0.y + 2*mt*t*lifeCtrlY + t*t*lifeP2.y;
    const x=snapX(bx, by, palmW*0.10);  // 検索幅を広げ実際のシワを拾う
    lifeRaw.push({x, y:cy2(by)});
  }

  // ─────────────────────────────────────────────
  // 【感情線】小指側〜人差し指付け根の横線（指付け根下14〜22%）
  // ─────────────────────────────────────────────
  const heartBaseY = fingerBaseY + palmH*0.17;
  const heartRaw = [];
  for (let i=0;i<=9;i++){
    const xC=cx2(xLeft+palmW*(i/9));
    const y=snapY(xC, heartBaseY, palmH*0.10);
    heartRaw.push({x:xC, y});
  }

  // ─────────────────────────────────────────────
  // 【知能線】生命線起点付近〜小指方向（感情線から15〜25%下）
  // ─────────────────────────────────────────────
  const headBaseY = fingerBaseY + palmH*0.34;
  const headRaw = [];
  for (let i=0;i<=8;i++){
    const t=i/8;
    const xC=cx2(lifeP0.x + (xRight-lifeP0.x)*t);
    const y=snapY(xC, headBaseY+palmH*0.06*t, palmH*0.11);
    headRaw.push({x:xC, y});
  }

  // ─────────────────────────────────────────────
  // 【運命線】手首中央〜中指付け根の縦線
  // ─────────────────────────────────────────────
  const fateRaw = [];
  for (let i=0;i<8;i++){
    const t=i/7;
    const sy=cy2(wristY+(fingerBaseY-wristY)*t);
    const x=snapX(centerX, sy, palmW*0.12);
    fateRaw.push({x, y:sy});
  }

  // ─────────────────────────────────────────────
  // 【太陽線】薬指(p[13])下の縦線
  // ─────────────────────────────────────────────
  const sunPts = [
    {x:cx2(p[13].x), y:cy2(fingerBaseY+palmH*0.12)},
    {x:cx2(p[13].x), y:cy2(fingerBaseY+palmH*0.52)}
  ];

  // ─────────────────────────────────────────────
  // 【結婚線】小指付け根の外側の短い横線
  // ─────────────────────────────────────────────
  const marY  = cy2(fingerBaseY+palmH*0.10);
  const marX0 = cx2(p[17].x+(flip?1:-1)*palmW*0.06);
  const marX1 = cx2(p[17].x-(flip?1:-1)*palmW*0.18);
  const marPts = [{x:marX0, y:marY},{x:marX1, y:marY}];

  // ─────────────────────────────────────────────
  // 【その他の線】骨格基準で固定
  // ─────────────────────────────────────────────
  const monX=cx2(lr(p[9],p[13],0.5).x);
  const popX=cx2(lr(p[13],p[17],0.38).x);
  const chX =cx2(lr(p[5],p[9],0.3).x);
  const devX=cx2(lr(p[1],p[5],0.3).x);

  return {
    life:     lifeRaw,
    heart:    heartRaw,
    head:     headRaw,
    fate:     fateRaw,
    sun:      sunPts,
    marriage: marPts,
    money:    [{x:monX,y:cy2(fingerBaseY+palmH*0.28)},{x:monX,y:cy2(fingerBaseY+palmH*0.62)}],
    popular:  [{x:popX,y:cy2(fingerBaseY+palmH*0.14)},{x:popX,y:cy2(fingerBaseY+palmH*0.42)}],
    charm:    [{x:chX, y:cy2(fingerBaseY+palmH*0.28)},{x:cx2(chX-(flip?-1:1)*palmH*0.04),y:cy2(fingerBaseY+palmH*0.50)}],
    devotion: [{x:devX,y:cy2(fingerBaseY+palmH*0.40)},{x:cx2(devX+(flip?-1:1)*palmH*0.05),y:cy2(fingerBaseY+palmH*0.60)}],
  };
}

// ===================== CREASE OVERLAY =====================
let creaseOverlayCache = null;

// Sobelエッジ検出 + 方向別NMSでシワを1ピクセル幅の鮮明な線として検出
function computeCreaseOverlay(canvas, lm) {
  const W = canvas.width, H = canvas.height;
  const raw = getGrayData(canvas);

  // 1) ガウシアンブラー（ノイズ除去、シワのエッジを保持）
  // カーネル: 5x5 近似
  const g = new Float32Array(W * H);
  const gk = [1,4,6,4,1, 4,16,24,16,4, 6,24,36,24,6, 4,16,24,16,4, 1,4,6,4,1];
  const gsum = 256;
  for (let y = 2; y < H-2; y++) {
    for (let x = 2; x < W-2; x++) {
      let s = 0, ki = 0;
      for (let dy = -2; dy <= 2; dy++)
        for (let dx = -2; dx <= 2; dx++)
          s += raw[(y+dy)*W+(x+dx)] * gk[ki++];
      g[y*W+x] = s / gsum;
    }
  }

  // 2) Sobelフィルタでエッジ強度と方向を計算
  const mag = new Float32Array(W * H);
  const ang = new Float32Array(W * H);
  for (let y = 1; y < H-1; y++) {
    for (let x = 1; x < W-1; x++) {
      const gx = -g[(y-1)*W+(x-1)] - 2*g[y*W+(x-1)] - g[(y+1)*W+(x-1)]
                + g[(y-1)*W+(x+1)] + 2*g[y*W+(x+1)] + g[(y+1)*W+(x+1)];
      const gy = -g[(y-1)*W+(x-1)] - 2*g[(y-1)*W+x] - g[(y-1)*W+(x+1)]
                + g[(y+1)*W+(x-1)] + 2*g[(y+1)*W+x] + g[(y+1)*W+(x+1)];
      mag[y*W+x] = Math.sqrt(gx*gx + gy*gy);
      ang[y*W+x] = Math.atan2(gy, gx);
    }
  }

  // 3) 手の範囲を決定
  let hMinX = 1, hMaxX = W-1, hMinY = 1, hMaxY = H-1;
  if (lm) {
    const p = lm.map(l => ({x: l.x*W, y: l.y*H}));
    const allX = p.map(pt=>pt.x), allY = p.map(pt=>pt.y);
    hMinX = Math.max(1, Math.min(...allX) - 15);
    hMaxX = Math.min(W-1, Math.max(...allX) + 15);
    hMinY = Math.max(1, Math.min(...allY) - 15);
    hMaxY = Math.min(H-1, Math.max(...allY) + 15);
  }

  // 4) 方向別NMS: エッジ方向に垂直な隣接ピクセルより大きければ残す
  // → 1ピクセル幅の正確なエッジ線になる
  const palmW = hMaxX - hMinX;
  // 閾値: 画像の解像度に応じて調整（手のひらの細かいシワを捉えるため低めに）
  const loThr = Math.max(8, palmW * 0.03);
  const hiThr = Math.max(20, palmW * 0.08);
  const thin = new Float32Array(W * H);

  for (let y = ~~hMinY + 1; y < ~~hMaxY - 1; y++) {
    for (let x = ~~hMinX + 1; x < ~~hMaxX - 1; x++) {
      const m = mag[y*W+x];
      if (m < loThr) continue;
      // エッジ方向を4方向に量子化して隣接比較
      const a = ang[y*W+x];
      let n1, n2;
      const ad = ((a * 180 / Math.PI) + 180) % 180;
      if (ad < 22.5 || ad >= 157.5) {
        n1 = mag[y*W+(x-1)]; n2 = mag[y*W+(x+1)];
      } else if (ad < 67.5) {
        n1 = mag[(y-1)*W+(x+1)]; n2 = mag[(y+1)*W+(x-1)];
      } else if (ad < 112.5) {
        n1 = mag[(y-1)*W+x]; n2 = mag[(y+1)*W+x];
      } else {
        n1 = mag[(y-1)*W+(x-1)]; n2 = mag[(y+1)*W+(x+1)];
      }
      if (m >= n1 && m >= n2) thin[y*W+x] = m;
    }
  }

  // 5) ダブル閾値 + エッジ連結（弱いエッジを強いエッジに隣接する場合のみ保持）
  const id = new ImageData(W, H);
  const strong = new Uint8Array(W * H);
  const weak   = new Uint8Array(W * H);
  for (let i = 0; i < W*H; i++) {
    if (thin[i] >= hiThr)      strong[i] = 1;
    else if (thin[i] >= loThr) weak[i]   = 1;
  }
  // 弱いエッジを連結（8近傍に強エッジがあれば採用）
  for (let y = ~~hMinY+1; y < ~~hMaxY-1; y++) {
    for (let x = ~~hMinX+1; x < ~~hMaxX-1; x++) {
      if (!weak[y*W+x]) continue;
      const hasStrong =
        strong[(y-1)*W+(x-1)] || strong[(y-1)*W+x] || strong[(y-1)*W+(x+1)] ||
        strong[y*W+(x-1)]     ||                       strong[y*W+(x+1)]     ||
        strong[(y+1)*W+(x-1)] || strong[(y+1)*W+x] || strong[(y+1)*W+(x+1)];
      if (hasStrong) strong[y*W+x] = 1;
    }
  }

  // 6) 最終描画: 強エッジを赤で描画
  for (let y = ~~hMinY; y < ~~hMaxY; y++) {
    for (let x = ~~hMinX; x < ~~hMaxX; x++) {
      if (!strong[y*W+x]) continue;
      const norm = Math.min(1, (thin[y*W+x] - loThr) / (hiThr * 2));
      const a = Math.round(200 + norm * 55);
      const idx = (y*W+x)*4;
      id.data[idx]   = 255;
      id.data[idx+1] = 30;
      id.data[idx+2] = 30;
      id.data[idx+3] = a;
    }
  }
  return id;
}

function applyCreaseOverlay(ctx, W, H) {
  if (!creaseOverlayCache) return;
  const tmp = document.createElement('canvas');
  tmp.width = W; tmp.height = H;
  tmp.getContext('2d').putImageData(creaseOverlayCache, 0, 0);
  ctx.drawImage(tmp, 0, 0);
}

// ===================== CANVAS =====================
function drawCanvas() {
  const canvas = document.getElementById('palmCanvas');
  const ctx = canvas.getContext('2d');
  const img = new Image();

  img.onerror = () => {
    alert('画像を読み込めませんでした。\nJPEGまたはPNG形式の写真をお試しください。');
  };
  img.onload = async () => {
    const maxW = Math.min(img.width, 900);
    const scale = maxW / img.width;
    canvas.width  = img.width  * scale;
    canvas.height = img.height * scale;
    ctx.drawImage(img, 0, 0, canvas.width, canvas.height);

    // ランドマーク検出（鑑定精度向上のために使用）
    const lm = await detectLandmarks(img);
    detectedLandmarkPaths = lm ? calcCreasePaths(lm, canvas) : null;

    // 手が検出された場合、手のひら領域をソフトに強調（写真をそのまま表示）
    if (lm) {
      drawHandGlow(ctx, lm, canvas.width, canvas.height);
    }

    buildLegend();
  };
  img.src = imgSrc;
}

// 手のひら検出済みを示すソフトな光彩エフェクト
function drawHandGlow(ctx, lm, W, H) {
  const p = lm.map(l => ({x: l.x*W, y: l.y*H}));
  // 手のひら中心
  const cx = (p[0].x + p[5].x + p[9].x + p[13].x + p[17].x) / 5;
  const cy = (p[0].y + p[5].y + p[9].y + p[13].y + p[17].y) / 5;
  const r  = Math.abs(p[0].y - p[9].y) * 0.6;

  const grad = ctx.createRadialGradient(cx, cy, r*0.3, cx, cy, r*1.4);
  grad.addColorStop(0, 'rgba(212,168,67,0.10)');
  grad.addColorStop(1, 'rgba(212,168,67,0)');
  ctx.save();
  ctx.fillStyle = grad;
  ctx.fillRect(0, 0, W, H);
  ctx.restore();
}

function redrawCanvas() {
  const canvas = document.getElementById('palmCanvas');
  const ctx = canvas.getContext('2d');
  const img = new Image();
  img.onload = () => {
    ctx.clearRect(0, 0, canvas.width, canvas.height);
    ctx.drawImage(img, 0, 0, canvas.width, canvas.height);
    if (detectedLandmarkPaths) {
      const lm = null; // グロー再描画（lm不要）
    }
  };
  img.src = imgSrc;
}

function buildLegend() {
  const wrap = document.getElementById('lineLegend');
  wrap.innerHTML = '';

  // 検出状況バッジ
  const detected = !!detectedLandmarkPaths;
  const badge = document.createElement('div');
  badge.className = 'detect-badge ' + (detected ? 'detected' : 'not-detected');
  badge.innerHTML = detected
    ? '✋ 手のひらを検出しました — 鑑定精度：高'
    : '🔍 手のひらを検出中… — 標準鑑定モードで分析';
  wrap.appendChild(badge);

  // 鑑定した線の一覧（アイコンのみ、タップ不要）
  const lineRow = document.createElement('div');
  lineRow.className = 'line-icons-row';
  PALM_LINES.forEach(l => {
    const chip = document.createElement('div');
    chip.className = 'line-icon-chip';
    chip.innerHTML = `<span class="line-chip-icon">${l.icon}</span><span class="line-chip-name">${l.name}</span>`;
    lineRow.appendChild(chip);
  });
  wrap.appendChild(lineRow);
}

// ===================== SCORES =====================
const SCORE_ITEMS = [
  { id:'life',    emoji:'❤️', label:'生命力' },
  { id:'heart',   emoji:'💕', label:'恋愛運' },
  { id:'money',   emoji:'💰', label:'金運' },
  { id:'fate',    emoji:'⭐', label:'仕事運' },
  { id:'charm',   emoji:'🌹', label:'モテ度' },
  { id:'popular', emoji:'✨', label:'人気運' },
  { id:'head',    emoji:'🧠', label:'知性' },
  { id:'marriage',emoji:'💍', label:'結婚運' },
];

function buildScores() {
  const grid = document.getElementById('scoresGrid');
  grid.innerHTML = '';
  SCORE_ITEMS.forEach((item, i) => {
    const s = scores[item.id] || 75;
    const el = document.createElement('div');
    el.className = 'score-card stagger-in';
    el.style.animationDelay = (i * 0.05) + 's';
    el.innerHTML = `
      <span class="score-emoji">${item.emoji}</span>
      <div class="score-label">${item.label}</div>
      <div class="score-value">${s}<span>点</span></div>
      <div class="score-bar"><div class="score-fill" data-w="${s}"></div></div>
    `;
    grid.appendChild(el);
  });
}

// ===================== READINGS =====================
function buildReadings() {
  const list = document.getElementById('readingsGrid');
  list.innerHTML = '';
  PALM_LINES.forEach((l, i) => {
    const el = document.createElement('div');
    el.className = 'reading-row stagger-in';
    el.style.animationDelay = (i * 0.05) + 's';
    el.innerHTML = `
      <div class="reading-icon-wrap">${l.icon}</div>
      <div class="reading-body">
        <div class="reading-name">${l.name}</div>
        <div class="reading-name-en">${l.nameEn}</div>
        <div class="reading-text">${readingTexts[l.id]}</div>
      </div>
    `;
    list.appendChild(el);
  });
}

// ===================== PERSONALITY =====================
function buildPersonality() {
  const el = document.getElementById('personalityText');
  const txt = PERSONALITIES[Math.floor(Math.random() * PERSONALITIES.length)];
  el.innerHTML = txt.split('\n\n').map(p => `<p>${p}</p>`).join('');
}

// ===================== FUTURE =====================
function buildFuture() {
  const list = document.getElementById('futureTimeline');
  list.innerHTML = '';

  // ── タイムライン予測 ──
  const timelineLabel = document.createElement('div');
  timelineLabel.className = 'panel-section-title';
  timelineLabel.textContent = 'これから起こりそうなこと';
  list.appendChild(timelineLabel);

  FUTURES.forEach((f, i) => {
    const el = document.createElement('div');
    el.className = 'future-card stagger-in';
    el.style.animationDelay = (i * 0.07) + 's';
    el.innerHTML = `
      <div class="future-badge">
        <span class="future-badge-label">今後</span>
        <span class="future-badge-val">${f.period}</span>
        <span class="future-badge-label">${f.label}</span>
      </div>
      <div class="future-body">
        <h4>${f.title}</h4>
        <p>${f.text}</p>
      </div>
    `;
    list.appendChild(el);
  });

  // ── 人生詳細鑑定カード ──
  const lifeLabel = document.createElement('div');
  lifeLabel.className = 'panel-section-title life-section-label';
  lifeLabel.textContent = '人生詳細鑑定';
  list.appendChild(lifeLabel);

  LIFE_READINGS.forEach((cat, i) => {
    const v = lifeReadingData[cat.id];
    if (!v) return;
    const el = document.createElement('div');
    el.className = 'life-card stagger-in';
    el.style.animationDelay = ((FUTURES.length + i) * 0.06) + 's';
    el.innerHTML = `
      <div class="life-card-header">
        <div class="life-card-icon">${cat.icon}</div>
        <div class="life-card-titles">
          <div class="life-card-badge">${cat.badge}</div>
          <div class="life-card-title">${cat.title}</div>
          <div class="life-card-headline">${v.headline}</div>
        </div>
        <div class="life-card-arrow">›</div>
      </div>
      <div class="life-card-body" hidden>
        <p class="life-card-text">${v.text}</p>
        <div class="life-advice-wrap">
          <div class="life-advice-label">✦ アドバイス</div>
          ${v.advice.map(a => `<div class="life-advice-item"><span class="life-advice-dot">◆</span><span>${a}</span></div>`).join('')}
        </div>
      </div>
    `;
    // タップで展開/折りたたみ
    el.querySelector('.life-card-header').addEventListener('click', () => {
      const body = el.querySelector('.life-card-body');
      const arrow = el.querySelector('.life-card-arrow');
      const open = body.hidden;
      body.hidden = !open;
      arrow.textContent = open ? '⌄' : '›';
      el.classList.toggle('open', open);
      if (open) el.scrollIntoView({ behavior:'smooth', block:'nearest' });
    });
    list.appendChild(el);
  });
}

// ===================== DETAIL =====================
function buildDetailOptions() {
  const grid = document.getElementById('detailOptions');
  grid.innerHTML = '';
  PALM_LINES.forEach(l => {
    const btn = document.createElement('button');
    btn.className = 'detail-chip';
    btn.dataset.id = l.id;
    btn.innerHTML = `${l.icon} ${l.name}`;
    btn.addEventListener('click', () => {
      document.querySelectorAll('.detail-chip').forEach(b => b.classList.remove('selected'));
      btn.classList.add('selected');
      showDeepReading(l.id);
    });
    grid.appendChild(btn);
  });
}

function showDeepReading(id) {
  const line = PALM_LINES.find(l => l.id === id);
  const data = READINGS[id];
  const score = scores[id] || 80;
  const stars = Math.round(score / 20);
  const starStr = '★'.repeat(stars) + '☆'.repeat(5 - stars);
  const labels = ['やや弱め','普通','やや強め','強い','非常に強い'];
  const label = labels[Math.min(stars - 1, 4)];

  const el = document.getElementById('deepReading');
  el.innerHTML = `
    <div class="deep-card">
      <div class="deep-top">
        <div class="deep-top-icon">${line.icon}</div>
        <div>
          <div class="deep-top-title">${data.deep.title}</div>
          <div class="deep-top-en">${line.nameEn} — 詳細分析</div>
        </div>
      </div>
      <div class="deep-body">
        <div class="deep-score-row">
          <div class="deep-score-num">${score}<span>点</span></div>
          <div>
            <div class="deep-stars">${starStr}</div>
            <div class="deep-rating-label">${label}</div>
          </div>
        </div>
        <div class="deep-section-label">詳細分析</div>
        <div class="deep-text">${data.deep.content.map(p => `<p>${p}</p>`).join('')}</div>
        <div class="advice-block">
          <div class="deep-section-label">あなたへのアドバイス</div>
          ${data.deep.advice.map(a => `<div class="advice-item"><div class="advice-dot"></div><span>${a}</span></div>`).join('')}
        </div>
      </div>
    </div>
  `;

  el.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
}

// ===================== TABS =====================
function setupTabs() {
  document.querySelectorAll('.tab-item').forEach(btn => {
    btn.addEventListener('click', () => {
      switchTab(btn.dataset.tab);
    });
  });
}

function switchTab(tabName) {
  currentTab = tabName;

  // Update tab buttons
  document.querySelectorAll('.tab-item').forEach(btn => {
    btn.classList.toggle('active', btn.dataset.tab === tabName);
  });

  // Update panels
  document.querySelectorAll('.tab-panel').forEach(panel => {
    const isActive = panel.id === 'tab' + tabName;
    panel.classList.toggle('active', isActive);
  });

  // Trigger score bar animation when switching to Scores tab
  if (tabName === 'Scores') {
    setTimeout(() => {
      document.querySelectorAll('.score-fill').forEach(el => {
        el.style.width = el.dataset.w + '%';
      });
    }, 100);
  }
}

// ===================== RESET =====================
function resetToUpload() {
  const uploadScreen = document.getElementById('screenUpload');
  const resultScreen = document.getElementById('screenResult');

  resultScreen.classList.remove('active');
  uploadScreen.classList.add('active');

  document.getElementById('tabBar').style.display = 'none';
  document.getElementById('retakeBtn').style.opacity = '0';
  document.getElementById('retakeBtn').style.pointerEvents = 'none';
  document.getElementById('fileInput').value = '';
  document.getElementById('deepReading').innerHTML = '';
  document.querySelectorAll('.score-fill').forEach(el => el.style.width = '0');
  document.querySelectorAll('.detail-chip').forEach(el => el.classList.remove('selected'));

  imgSrc = null;
  lineVis = {};
  scores = {};
  readingTexts = {};
  lifeReadingData = {};
  detectedLandmarkPaths = null;
}

// ===================== LOADING =====================
function showLoading() { document.getElementById('loadingScreen').classList.add('active'); }
function hideLoading() { document.getElementById('loadingScreen').classList.remove('active'); }
